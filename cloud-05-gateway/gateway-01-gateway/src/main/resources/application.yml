server:
  port: 80
spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      routes:
        - id: login-server-route
          uri: http://localhost:8080
          predicates:
            - Path=/login
